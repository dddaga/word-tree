---
phase: 05-scalable-architecture-experiments
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - src/sgnnet/model_reflection.py
  - scripts/train_exp4_reflection.py
  - results/exp4_reflection.json
  - results/arch_comparison.md
  - results/arch_comparison.json
  - learnings/LEARNINGS_phase5_p13_reflection.md
autonomous: true
requirements: [SCALE-05, SCALE-06]
must_haves:
  truths:
    - "Signal reflection routing ablation complete at N=1024 (4 configs)"
    - "Architecture comparison table covers SmallWorld, ProximityWave, Reflection at multiple N"
    - "Leaky-reflect variant has <5% dead neurons"
  artifacts:
    - path: "src/sgnnet/model_reflection.py"
      provides: "Reflection routing mechanism"
    - path: "results/exp4_reflection.json"
      provides: "4-config reflection ablation results"
    - path: "results/arch_comparison.md"
      provides: "Full Phase 5 architecture comparison table"
  key_links:
    - from: "results/arch_comparison.md"
      to: "results/train_step56_n_scaling.json"
      via: "SmallWorld scaling data"
    - from: "results/arch_comparison.md"
      to: "results/exp3_proxwave.json"
      via: "ProximityWave data"
---

<objective>
Signal reflection routing experiment + architecture comparison table assembling SmallWorld vs ProximityWave vs Reflection results.

Purpose: Test whether sign-based conditional propagation (reflection) creates useful input-dependent routing; assemble the Track A architecture comparison deliverable.
Output: Reflection model, ablation results, architecture comparison table (arch_comparison.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md
@src/sgnnet/model_smallworld.py
@src/sgnnet/model_proximity_wave.py
@.planning/phases/05-scalable-architecture-experiments/05-01-SUMMARY.md
@.planning/phases/05-scalable-architecture-experiments/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement reflection routing and run ablation</name>
  <files>src/sgnnet/model_reflection.py, scripts/train_exp4_reflection.py</files>
  <read_first>
    - src/sgnnet/model_smallworld.py (forward pass structure, gather-sum pattern)
    - src/sgnnet/mechanisms_inhibitory.py (how AntiHebbian wraps a base model)
    - src/training/trainer.py (Trainer interface)
  </read_first>
  <action>
1. Create `src/sgnnet/model_reflection.py`:
   - Class SGNNET_Reflection(nn.Module) — wraps any base model (SmallWorld or ProximityWave)
   - Constructor args: base_model, alpha_reflect=0.1, theta=0.0
   - Modifies the hidden iteration step:
     ```python
     Z_prop = torch.relu(Z)                           # positive activations propagate
     Z_reflect = alpha_reflect * torch.relu(-Z - theta) # strongly negative bounce back
     Z_new = -Z_reflect + gather_sum(Z_prop, conn_hh)  # self-inhibition + neighbor propagation
     ```
   - Tracks dead neuron fraction per step: `(Z.abs() < 1e-6).float().mean()` — log as diagnostic
   - If dead_frac > 0.05 at any step, log warning (dying neuron cascade risk)

2. Write `scripts/train_exp4_reflection.py`:
   - 4 configs at N=1024, D=64, K_iter=8, 100 epochs each:
     - Config A: SmallWorld + AntiHebb alpha=0.7 (reference, no reflection)
     - Config B: SmallWorld + AntiHebb alpha=0.7 + leaky-reflect (alpha_reflect=0.1, theta=0.0)
     - Config C: SmallWorld + AntiHebb alpha=0.7 + hard-reflect (alpha_reflect=1.0, theta=0.5)
     - Config D: ProximityWave + AntiHebb alpha=0.7 + leaky-reflect (alpha_reflect=0.1, theta=0.0)
   - Log per-epoch: loss, top1, dead_neuron_frac
   - Save to results/exp4_reflection.json

3. Dispatch to Mac Studio (check concurrency first), monitor, sync results.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('src/sgnnet/model_reflection.py').read()); ast.parse(open('scripts/train_exp4_reflection.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/sgnnet/model_reflection.py exists with SGNNET_Reflection class
    - Class has alpha_reflect and theta constructor args
    - scripts/train_exp4_reflection.py defines 4 configs (A, B, C, D)
    - Dead neuron tracking is present in forward pass
  </acceptance_criteria>
  <done>Reflection routing implemented; 4-config ablation dispatched</done>
</task>

<task type="auto">
  <name>Task 2: Assemble architecture comparison table from all Track A results</name>
  <files>results/arch_comparison.md, results/arch_comparison.json, learnings/LEARNINGS_phase5_p13_reflection.md</files>
  <read_first>
    - results/train_step56_n_scaling.json (Plan 05-01: SmallWorld scaling data)
    - results/exp3_proxwave.json (Plan 05-02: ProximityWave data)
    - results/exp4_reflection.json (Task 1: reflection ablation data)
    - results/train_step29_antihebb_d64.json (existing best: AntiHebb 75.24%)
  </read_first>
  <action>
1. Collect all results into `results/arch_comparison.json`:
   ```json
   {
     "rows": [
       {"arch": "SmallWorld baseline", "N": 1024, "top1": 56.28, "mechanisms": "none", "source": "step22"},
       {"arch": "SmallWorld + AntiHebb", "N": 1024, "top1": 75.24, "mechanisms": "AntiHebb a=0.7", "source": "step29C"},
       {"arch": "SmallWorld + AntiHebb", "N": 512, "top1": "...", "source": "step56"},
       {"arch": "SmallWorld + AntiHebb", "N": 2048, "top1": "...", "source": "step56"},
       {"arch": "SmallWorld + AntiHebb", "N": 4096, "top1": "...", "source": "step56"},
       {"arch": "SmallWorld + AntiHebb", "N": 10000, "top1": "...", "source": "step56"},
       {"arch": "ProximityWave + AntiHebb", "N": 1024, "top1": "...", "source": "exp3"},
       {"arch": "ProximityWave + AntiHebb", "N": 4096, "top1": "...", "source": "exp3"},
       {"arch": "SmallWorld + AntiHebb + leaky-reflect", "N": 1024, "top1": "...", "source": "exp4B"},
       {"arch": "SmallWorld + AntiHebb + hard-reflect", "N": 1024, "top1": "...", "source": "exp4C"},
       {"arch": "ProximityWave + AntiHebb + leaky-reflect", "N": 1024, "top1": "...", "source": "exp4D"}
     ]
   }
   ```
   Fill actual numbers from JSONs.

2. Generate `results/arch_comparison.md`:
   ```
   # Phase 5 Architecture Comparison

   | Architecture | N | top-1 | Params | ms/epoch | Key Mechanism |
   |---|---|---|---|---|---|
   | SmallWorld baseline | 1024 | 56.28% | ... | ... | none |
   | SmallWorld + AntiHebb | 1024 | 75.24% | ... | ... | AntiHebb a=0.7 |
   ... (all rows from JSON)
   ```

3. Write `learnings/LEARNINGS_phase5_p13_reflection.md`:
   - Reflection ablation results (4 configs)
   - Dead neuron analysis (leaky vs hard)
   - Architecture ranking: which topology wins at each N?
   - Recommendation for best architecture going forward
  </action>
  <verify>
    <automated>test -f results/arch_comparison.md && test -f results/arch_comparison.json && python -c "import json; d=json.load(open('results/arch_comparison.json')); assert len(d.get('rows',d)) >= 8; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - results/arch_comparison.json exists with >= 8 architecture rows
    - results/arch_comparison.md exists with markdown table
    - Table includes SmallWorld, ProximityWave, and Reflection variants
    - learnings/LEARNINGS_phase5_p13_reflection.md documents reflection findings
  </acceptance_criteria>
  <done>Architecture comparison table produced; reflection findings documented; Track A complete</done>
</task>

</tasks>

<verification>
- Reflection ablation has 4 configs with results
- Leaky-reflect dead neuron fraction < 5%
- Architecture comparison table has >= 8 rows spanning 3 architectures
- LEARNINGS documents findings
</verification>

<success_criteria>
Track A architectural exploration complete: SmallWorld scaling + ProximityWave + Reflection all compared; best architecture identified per N.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalable-architecture-experiments/05-03-SUMMARY.md`
</output>
