---
phase: 05-scalable-architecture-experiments
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - results/train_step36_input_gated.json
  - results/train_step49_signed_kiter_threshold.json
  - results/train_step50_spatial_dynamic_conn.json
  - results/train_step51_spatial_phase_gating.json
  - results/train_step55_spatial_grouped_input.json
  - results/pca_input_sweep.json
  - scripts/pca_input_sweep.py
  - learnings/LEARNINGS_phase5_p9_arm3_arm5.md
autonomous: true
requirements: [ARM-03, ARM-05]
must_haves:
  truths:
    - "Dynamic connectivity question closed: go/no-go verdict on O(N*K) input-dependent topology at D=64"
    - "All dynamic connectivity experiments (step36 final, step49, step50, step51) have results synced and analyzed"
    - "step55 spatial grouped input dispatched and results analyzed"
    - "PCA input sweep (k=256,512,1024) completed and compared vs random K_in=50 vs spatial grouped"
    - "Best input mechanism identified and documented"
  artifacts:
    - path: "results/train_step36_input_gated.json"
      provides: "Input-gated adjacency final results (all configs)"
    - path: "results/train_step49_signed_kiter_threshold.json"
      provides: "Signed coupling K_iter threshold sweep"
    - path: "results/train_step50_spatial_dynamic_conn.json"
      provides: "Spatial W_pos K-NN dynamic connectivity"
    - path: "results/train_step51_spatial_phase_gating.json"
      provides: "W_pos K-NN + W_phase strength gating"
    - path: "results/train_step55_spatial_grouped_input.json"
      provides: "Spatial grouped learned input projection (6 configs)"
    - path: "results/pca_input_sweep.json"
      provides: "PCA compressed input at k=256,512,1024"
    - path: "learnings/LEARNINGS_phase5_p9_arm3_arm5.md"
      provides: "Dynamic connectivity verdict + best input mechanism"
  key_links:
    - from: "results/train_step49_signed_kiter_threshold.json"
      to: "learnings/LEARNINGS_phase5_p9_arm3_arm5.md"
      via: "K_iter sweet spot for signed coupling (or confirmation it's dead at all depths)"
    - from: "results/train_step55_spatial_grouped_input.json"
      to: "learnings/LEARNINGS_phase5_p9_arm3_arm5.md"
      via: "Spatial grouped vs random K_in=50 comparison"
    - from: "results/pca_input_sweep.json"
      to: "learnings/LEARNINGS_phase5_p9_arm3_arm5.md"
      via: "PCA input vs other input mechanisms"
---

<objective>
ARM 3 (Dynamic Connectivity Closure) + ARM 5 (Input Architecture): Resolve whether O(N*K) input-dependent topology can match signed coupling at D=64. Test spatial grouped input and PCA compression as alternatives to random K_in=50 gather.

Purpose: Close the two remaining architectural questions before Phase 5 synthesis. Dynamic connectivity determines whether the static SmallWorld topology is final or if input-dependent routing adds value. Input architecture determines the optimal front-end for the 25088-dim VGG16 features.
Output: All ARM 3 dynamic connectivity results synced and verdict rendered; ARM 5 input mechanism winner identified; LEARNINGS documenting both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md
@learnings/EXPERIMENT_QUEUE.md
@learnings/LEARNINGS_phase5_p7_gen3_arm_status.md
@results/train_step36_input_gated.json
@scripts/train_step49_signed_kiter_threshold.py
@scripts/train_step50_spatial_dynamic_conn.py
@scripts/train_step51_spatial_phase_gating.py
@scripts/train_step55_spatial_grouped_input.py
@src/sgnnet/model_spatial_grouped.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch and analyze all ARM 3 dynamic connectivity experiments (step36/49/50/51)</name>
  <files>
    results/train_step36_input_gated.json,
    results/train_step49_signed_kiter_threshold.json,
    results/train_step50_spatial_dynamic_conn.json,
    results/train_step51_spatial_phase_gating.json
  </files>
  <read_first>
    - .planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md (SSH details, concurrency rules, step36 current status)
    - learnings/EXPERIMENT_QUEUE.md (ARM 3 section: step31/36/49/50/51 designs, decision rules, N-squared recovery metric)
    - results/train_step36_input_gated.json (partial: Config 1 = 58.62%, other configs pending)
    - scripts/train_step49_signed_kiter_threshold.py (signed coupling K_iter={3,4,5,6,7,8} sweep design)
    - scripts/train_step50_spatial_dynamic_conn.py (spatial W_pos K-NN connectivity)
    - scripts/train_step51_spatial_phase_gating.py (W_pos K-NN + W_phase strength gating, Bragg analogy)
  </read_first>
  <action>
**Phase A — Sync step36 final results:**

1. Check step36 status on Mac Studio:
   ```
   ssh mac-studio "tail -20 /Users/admin/ml/dhiraj/qwen2_omni/testing/logs/train_step36_input_gated.log"
   ```

2. When complete, sync all configs:
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step36_input_gated.json results/
   ```

3. Analyze step36: Config 1 already = 58.62% (+2.34pp over 56.28% ceiling). Do gated configs improve further? Record all config results. This is the strongest dynamic connectivity signal so far.

**Phase B — Dispatch step49, step50, step51 sequentially (concurrency cap):**

4. For each of step49, step50, step51 IN ORDER (respecting Mac Studio concurrency cap of 2):

   a. Check concurrency: `ssh mac-studio 'ps aux | grep "train_step" | grep -v grep | wc -l'` — must be <= 1
   b. Check RAM: `ssh mac-studio "vm_stat | awk '/Pages free|Pages inactive/ {sum+=\$NF} END {printf \"%.0f GB\n\", sum*16384/1073741824}'"` — must be >= 50 GB
   c. Rsync script:
      ```
      rsync -avz scripts/train_step49_signed_kiter_threshold.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
      ```
   d. Launch in tmux:
      ```
      ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step49 'd_env/bin/python3 -u scripts/train_step49_signed_kiter_threshold.py --device mps 2>&1 | tee logs/train_step49_signed_kiter_threshold.log'"
      ```
   e. Monitor until complete, then sync result:
      ```
      rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step49_signed_kiter_threshold.json results/
      ```
   f. Repeat for step50 and step51 with same pattern (substitute script names).

   **IMPORTANT:** step49 is 40-epoch calibration runs (fast). step50 and step51 are 150-epoch full runs. Prioritize step49 first since it's fastest and resolves the signed coupling question directly.

5. Analyze step49: For K_iter={3,4,5,6,7,8} with signed coupling alpha=0.3:
   - Is there a viable K_iter where signed coupling works at D=64? (any config > 42% Ref_40ep)
   - Where does collapse start? (top1 < 15% = threshold)
   - If ALL configs fail: signed coupling is conclusively dead at D=64 at ALL depths. Document and close.

6. Analyze step50: Spatial W_pos K-NN (epoch-level rebuild):
   - Does learned geometry-based dynamic topology outperform static SmallWorld?
   - Compare vs step36 input-gated: which dynamic approach is better?

7. Analyze step51: W_pos K-NN + W_phase strength gating (Bragg analogy):
   - Does adding W_phase-based strength gating to spatial connectivity help?
   - Compare vs step50 (spatial alone) and step36 (input-gated alone).

8. Render ARM 3 verdict:
   - **GO:** If any O(N*K) dynamic topology config beats static SmallWorld + AntiHebb (75.24%) → adopt for Gen4
   - **PARTIAL GO:** If dynamic topology beats static ceiling (56.28%) but not AntiHebb best (75.24%) → document as complementary mechanism, test compounding with AntiHebb in step32
   - **NO-GO:** If all dynamic topologies fail to beat static ceiling → static SmallWorld is the final topology. Dynamic connectivity is dead at D=64.
  </action>
  <verify>
    <automated>python3 -c "
import os, json
results = {
    'step36': 'results/train_step36_input_gated.json',
    'step49': 'results/train_step49_signed_kiter_threshold.json',
    'step50': 'results/train_step50_spatial_dynamic_conn.json',
    'step51': 'results/train_step51_spatial_phase_gating.json'
}
found = {k: os.path.exists(v) for k, v in results.items()}
print(f'Results synced: {sum(found.values())}/4 — {found}')
if found['step36']:
    d = json.load(open(results['step36']))
    print(f'step36 entries: {len(d) if isinstance(d, list) else len(d.keys())}')
"</automated>
  </verify>
  <acceptance_criteria>
    - results/train_step36_input_gated.json exists with ALL configs (not just Config 1)
    - results/train_step49_signed_kiter_threshold.json exists with K_iter={3,4,5,6,7,8} results
    - results/train_step50_spatial_dynamic_conn.json exists with spatial K-NN results
    - results/train_step51_spatial_phase_gating.json exists with Bragg-gated results
    - No result JSON contains NaN values in top-1 accuracy fields
  </acceptance_criteria>
  <done>All 4 ARM 3 dynamic connectivity experiments synced with results; signed coupling K_iter threshold identified (or confirmed dead); spatial and gated dynamic topologies evaluated</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch step55 (spatial grouped input), run PCA input sweep, and write LEARNINGS</name>
  <files>
    results/train_step55_spatial_grouped_input.json,
    results/pca_input_sweep.json,
    scripts/pca_input_sweep.py,
    learnings/LEARNINGS_phase5_p9_arm3_arm5.md
  </files>
  <read_first>
    - .planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md (step55 status: script + model ready locally, not yet dispatched)
    - learnings/EXPERIMENT_QUEUE.md (ARM 5 section)
    - scripts/train_step55_spatial_grouped_input.py (6 configs: Ref / 7-rows-0% / 7-rows-20% / 7-rows-40% / 49-pos-20% / 8-flat-20%)
    - src/sgnnet/model_spatial_grouped.py (spatial grouped model architecture)
    - src/sgnnet/model_smallworld.py (SmallWorld conn_in pattern — random K_in=50 from 25088)
    - src/training/trainer.py (Trainer interface)
    - results/train_step29_antihebb_d64.json (75.24% reference for comparison)
  </read_first>
  <action>
**Phase A — Dispatch step55 (spatial grouped input):**

1. Rsync model and script to Mac Studio:
   ```
   rsync -avz src/sgnnet/model_spatial_grouped.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/src/sgnnet/
   rsync -avz scripts/train_step55_spatial_grouped_input.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   ```

2. Check concurrency (must be <= 1 AND RAM >= 50GB):
   ```
   ssh mac-studio 'ps aux | grep "train_step" | grep -v grep | wc -l'
   ssh mac-studio "vm_stat | awk '/Pages free|Pages inactive/ {sum+=\$NF} END {printf \"%.0f GB\n\", sum*16384/1073741824}'"
   ```

3. Launch step55:
   ```
   ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step55 'd_env/bin/python3 -u scripts/train_step55_spatial_grouped_input.py --device mps 2>&1 | tee logs/train_step55_spatial_grouped_input.log'"
   ```

4. Monitor and sync when complete:
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step55_spatial_grouped_input.json results/
   ```

5. Analyze step55: For 6 configs, compare top-1 accuracy:
   - Ref (random K_in=50): anchor ~75.24% with AntiHebb
   - Does any spatial grouped config beat Ref? Key question: does exploiting VGG16's [512,7,7] spatial structure in the input projection add value?
   - Best spatial config: which grouping (7-rows, 49-pos, 8-flat) and what bridge fraction (0%, 20%, 40%)?

**Phase B — Write and run PCA input sweep:**

6. Write `scripts/pca_input_sweep.py`:
   - Load training features from `data/store.h5` (train/features, shape [9469, 25088])
   - Fit sklearn PCA on training features for k in {256, 512, 1024}
   - Transform both train and val features to k dimensions
   - For each k: instantiate SGNNET_SmallWorld with N_in=k (instead of 25088), wrap with Resonant + AntiHebb alpha=0.7
   - Adjust K_in: K_in = min(50, k) since PCA output may be smaller than 50
   - Train 150 epochs each, batch=128, plateau LR
   - Save to results/pca_input_sweep.json: per-k top1, mAP, params, wall_time, explained_variance_ratio
   - Also record cumulative explained variance at each k for the variance curve

7. Dispatch PCA sweep (can run locally if Mac Studio is busy, since PCA is CPU-bound for fitting; or dispatch to Mac Studio for training):
   ```
   rsync -avz scripts/pca_input_sweep.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s pca_sweep 'd_env/bin/python3 -u scripts/pca_input_sweep.py --device mps 2>&1 | tee logs/pca_input_sweep.log'"
   ```

8. When complete, sync:
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/pca_input_sweep.json results/
   ```

**Phase C — Write LEARNINGS consolidating ARM 3 + ARM 5:**

9. Write `learnings/LEARNINGS_phase5_p9_arm3_arm5.md`:

   **ARM 3 — Dynamic Connectivity Closure:**
   - Table: all dynamic connectivity experiments (step31, step36, step49, step50, step51) with top-1, mechanism, cost
   - N-squared recovery metric: `(gain / 10.93pp) * 100%` for each O(N*K) approach
   - Verdict: GO / PARTIAL-GO / NO-GO with justification
   - If PARTIAL-GO: which dynamic mechanism to test compounding with AntiHebb in Gen4?
   - Comparison: input-gated (step36) vs spatial K-NN (step50) vs Bragg-gated (step51) vs signed coupling threshold (step49)
   - Final answer: "The best dynamic topology at O(N*K) complexity is X at Y% accuracy" or "No O(N*K) dynamic topology is viable at D=64"

   **ARM 5 — Input Architecture:**
   - Table: random K_in=50 vs spatial grouped (6 configs) vs PCA (3 k values) — top1, params, wall_time
   - Best input mechanism identified with reasoning
   - If PCA wins: record best k and explained variance ratio (feeds Phase 6 PCA-01 through PCA-06)
   - If spatial grouped wins: record best config and bridge fraction
   - If random K_in=50 wins: confirm current architecture is optimal; document why learned projections don't help

   **Cross-ARM insight:** Does the best dynamic topology compound with the best input mechanism?
  </action>
  <verify>
    <automated>python3 -c "
import os
files = {
    'step55': 'results/train_step55_spatial_grouped_input.json',
    'pca': 'results/pca_input_sweep.json',
    'learnings': 'learnings/LEARNINGS_phase5_p9_arm3_arm5.md',
    'pca_script': 'scripts/pca_input_sweep.py'
}
found = {k: os.path.exists(v) for k, v in files.items()}
print(f'Files present: {sum(found.values())}/4 — {found}')
if found['pca_script']:
    import ast; ast.parse(open('scripts/pca_input_sweep.py').read())
    print('pca_input_sweep.py parses OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - results/train_step55_spatial_grouped_input.json exists with 6 config results
    - results/pca_input_sweep.json exists with results for k=256, k=512, k=1024
    - scripts/pca_input_sweep.py exists and parses without syntax error
    - learnings/LEARNINGS_phase5_p9_arm3_arm5.md exists with both ARM 3 and ARM 5 sections
    - ARM 3 section contains explicit GO / PARTIAL-GO / NO-GO verdict
    - ARM 5 section identifies a single best input mechanism with comparison table
  </acceptance_criteria>
  <done>ARM 3 dynamic connectivity question closed with verdict; ARM 5 input architecture winner identified; all results synced; LEARNINGS consolidates findings from both ARMs</done>
</task>

</tasks>

<verification>
- All ARM 3 results synced: step36 (all configs), step49, step50, step51
- ARM 3 verdict rendered: go/partial-go/no-go on dynamic connectivity at D=64
- step55 spatial grouped input dispatched and results synced
- PCA input sweep completed with k=256,512,1024
- LEARNINGS documents both ARM 3 and ARM 5 with clear winners
- Best input mechanism identified (random K_in=50 vs spatial grouped vs PCA)
</verification>

<success_criteria>
Both remaining architectural questions resolved: (1) dynamic connectivity viability at O(N*K) complexity, and (2) optimal input architecture for 25088-dim VGG16 features. Results feed directly into Plan 05-06 synthesis for final Phase 5 best config.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalable-architecture-experiments/05-05-SUMMARY.md`
</output>
