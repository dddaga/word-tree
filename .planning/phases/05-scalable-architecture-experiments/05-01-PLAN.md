---
phase: 05-scalable-architecture-experiments
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train_step56_n_scaling.py
  - src/sgnnet/losses.py
  - src/training/experiment_config.py
  - results/train_step56_n_scaling.json
  - learnings/LEARNINGS_phase5_p11_n_scaling.md
autonomous: true
requirements: [SCALE-01, SCALE-02]
must_haves:
  truths:
    - "Accuracy vs N curve exists for N=[512,1024,2048,4096,10000] at D=64 K_iter=8 AntiHebb alpha=0.7"
    - "No OOM crash at N=10000"
    - "Results JSON records top-1 per N plus params and wall-clock time"
  artifacts:
    - path: "scripts/train_step56_n_scaling.py"
      provides: "N scaling sweep script with all 5 N values"
    - path: "results/train_step56_n_scaling.json"
      provides: "Per-N accuracy, params, time"
    - path: "learnings/LEARNINGS_phase5_p11_n_scaling.md"
      provides: "Scaling analysis: where does performance plateau?"
  key_links:
    - from: "scripts/train_step56_n_scaling.py"
      to: "src/sgnnet/model_smallworld.py"
      via: "SGNNET_SmallWorld import"
    - from: "scripts/train_step56_n_scaling.py"
      to: "src/sgnnet/mechanisms_inhibitory.py"
      via: "SGNNET_AntiHebbian wrapper"
---

<objective>
Neuron scaling sweep: run SGNNET_SmallWorld + AntiHebb alpha=0.7 wpos at N=[512, 1024, 2048, 4096, 10000] to establish accuracy vs compute trade-off at scale.

Purpose: Determine if increasing N beyond 1024 improves accuracy or plateaus; identify OOM/time constraints at large N.
Output: Scaling sweep JSON, LEARNINGS entry with accuracy-vs-N curve analysis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md
@src/sgnnet/model_smallworld.py
@src/sgnnet/mechanisms_inhibitory.py
@src/training/experiment_config.py
@src/sgnnet/losses.py
@learnings/EXPERIMENT_QUEUE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write N-scaling sweep script with bug fixes</name>
  <files>scripts/train_step56_n_scaling.py, src/sgnnet/losses.py, src/training/experiment_config.py</files>
  <read_first>
    - src/sgnnet/model_smallworld.py (SmallWorld constructor args: N_hidden, D, K_in, K_local, K_random, n_groups)
    - src/sgnnet/mechanisms_inhibitory.py (AntiHebbian wrapper args: model, alpha, variant)
    - src/sgnnet/model_resonant.py (Resonant wrapper args: model, mode, beam_size, K_phase)
    - src/training/trainer.py (Trainer class, train method signature)
    - src/training/experiment_config.py (topology_kwargs, trainer_kwargs, GA_BEST)
    - src/sgnnet/losses.py (safety_valve_loss — check for fill_diagonal_ in-place and boolean indexing)
    - scripts/train_step29_antihebb_d64.py (reference for how existing experiment scripts are structured)
  </read_first>
  <action>
1. Fix bugs in `src/sgnnet/losses.py` if present:
   - Replace `strength.fill_diagonal_(0)` with `strength = strength * (1 - torch.eye(N, device=strength.device))` (out-of-place for MPS compatibility)
   - Replace `dists[mask]` boolean indexing with element-wise masking: `masked_dists = dists * mask.float()`
   - Add OOM guard: if N > 5000, skip safety_valve_loss entirely (return 0.0) since pairwise distance matrix is too large

2. Update `src/training/experiment_config.py`:
   - Add `lambda_safety` scaling: `lambda_safety * (256 / N_hidden) ** (1.0 / D)` to compensate for denser packing at large N
   - Ensure GA_BEST reflects current best: top1=75.24% from step29 Config C

3. Write `scripts/train_step56_n_scaling.py`:
   - Iterate N_hidden = [512, 1024, 2048, 4096, 10000]
   - For each N: instantiate SGNNET_SmallWorld(N_hidden=N, D=64, K_in=50, K_local=4, K_random=2, n_groups=min(128, N//8))
   - Wrap with SGNNET_Resonant(mode='dynamic_z_geo', beam_size=32, K_phase=8)
   - Wrap with SGNNET_AntiHebbian(alpha=0.7, variant='wpos')
   - Train 150 epochs, batch=128, plateau LR (patience=10, factor=0.5)
   - Save per-N: top1, mAP, param_count, wall_time_minutes to a list
   - Write all results to `results/train_step56_n_scaling.json`
   - Each N runs sequentially (not parallel — large N needs full GPU)
   - Use `d_env/bin/python3 -u` for unbuffered output; `--device mps`
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/train_step56_n_scaling.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/train_step56_n_scaling.py exists and parses without syntax error
    - Script references all 5 N values: 512, 1024, 2048, 4096, 10000
    - Script imports SGNNET_SmallWorld, SGNNET_Resonant, SGNNET_AntiHebbian
    - losses.py has no fill_diagonal_ calls (grep confirms)
    - losses.py has OOM guard for N > 5000
  </acceptance_criteria>
  <done>Script written; losses.py bugs fixed; experiment_config.py updated with lambda_safety scaling</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch N-scaling sweep to Mac Studio and monitor</name>
  <files>results/train_step56_n_scaling.json, learnings/LEARNINGS_phase5_p11_n_scaling.md</files>
  <read_first>
    - .planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md (SSH details, concurrency rules)
    - learnings/EXPERIMENT_QUEUE.md (currently running experiments — check slot availability)
  </read_first>
  <action>
1. Check Mac Studio concurrency: `ssh mac-studio 'ps aux | grep train_step | grep -v grep | wc -l'` — must be <= 1 AND RAM >= 50GB
   - RAM check: `ssh mac-studio "vm_stat | awk '/Pages free|Pages inactive/ {sum+=\$NF} END {printf \"%.0f GB\", sum*16384/1073741824}'"` 

2. Rsync files to Mac Studio:
   ```
   rsync -avz scripts/train_step56_n_scaling.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   rsync -avz src/sgnnet/losses.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/src/sgnnet/
   rsync -avz src/training/experiment_config.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/src/training/
   ```

3. Launch in tmux: `ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step56_nscale 'd_env/bin/python3 -u scripts/train_step56_n_scaling.py --device mps 2>&1 | tee logs/train_step56_n_scaling.log'"`

4. Monitor periodically: `ssh mac-studio "tail -20 /Users/admin/ml/dhiraj/qwen2_omni/testing/logs/train_step56_n_scaling.log"`

5. When complete, sync results: `rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step56_n_scaling.json results/`

6. Write `learnings/LEARNINGS_phase5_p11_n_scaling.md`:
   - Table: N vs top-1 vs params vs wall-time
   - Analysis: where does accuracy plateau? Is N=4096 or N=10000 worth the compute?
   - Recommendation for best N value going forward
  </action>
  <verify>
    <automated>test -f results/train_step56_n_scaling.json && python -c "import json; d=json.load(open('results/train_step56_n_scaling.json')); assert len(d) >= 5, f'only {len(d)} results'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - results/train_step56_n_scaling.json exists with entries for all 5 N values
    - Each entry has keys: N_hidden, top1, params, wall_time
    - learnings/LEARNINGS_phase5_p11_n_scaling.md exists with accuracy-vs-N table
    - No NaN values in any result
  </acceptance_criteria>
  <done>All 5 N values trained to 150 epochs; results synced; LEARNINGS documents accuracy plateau analysis</done>
</task>

</tasks>

<verification>
- `results/train_step56_n_scaling.json` contains 5 entries (one per N value)
- Each entry has top1, params, wall_time fields
- N=10000 did not OOM
- LEARNINGS file documents where accuracy plateaus
</verification>

<success_criteria>
Accuracy vs N curve established for N=[512,1024,2048,4096,10000]; plateau point identified; results feed into Plan 05-03 architecture comparison and Plan 05-06 synthesis.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalable-architecture-experiments/05-01-SUMMARY.md`
</output>
