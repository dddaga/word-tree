<!-- Updated by new architecture experiments step57-61 (2026-04-04): steps 57-61 run as a fast ablation wave (50% data, 75ep) BEFORE this plan's step32 Gen4 synthesis. Winners from 58-61 feed into Gen4 compound. -->
---
phase: 05-scalable-architecture-experiments
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/train_step53_lowrank_mixing.py
  - scripts/train_step32_gen4_compound.py
  - results/train_step29c_mechanisms_calibrated.json
  - results/train_step48_kiter_sweep_d64.json
  - results/train_step54_warm_restart_lr.json
  - results/train_step52_high_d_routing.json
  - results/train_step53_lowrank_mixing.json
  - results/train_step32_gen4_compound.json
  - learnings/LEARNINGS_phase5_p8_arm1_arm2.md
  - src/training/experiment_config.py
autonomous: true
requirements: [ARM-01, ARM-02]
must_haves:
  truths:
    - "step29c mechanisms_calibrated results synced and analyzed: which mechanisms survive on calibrated base"
    - "step48 K_iter sweep results synced: whether K_iter > 8 helps at D=64"
    - "step54 LR schedule results synced: plateau vs CosineWarmRestarts vs cosine compared"
    - "step52 high-D routing dispatched and analyzed"
    - "step53 low-rank mixing script written, dispatched, and analyzed"
    - "Gen4 compound config (step32) identified from ARM 1+2 winners and dispatched"
    - "Gen4 ceiling accuracy documented in LEARNINGS"
  artifacts:
    - path: "results/train_step29c_mechanisms_calibrated.json"
      provides: "Mechanism survival on calibrated base"
    - path: "results/train_step48_kiter_sweep_d64.json"
      provides: "K_iter scaling results"
    - path: "results/train_step54_warm_restart_lr.json"
      provides: "LR schedule comparison"
    - path: "results/train_step32_gen4_compound.json"
      provides: "Gen4 compound ceiling"
    - path: "learnings/LEARNINGS_phase5_p8_arm1_arm2.md"
      provides: "ARM 1+2 analysis: winners, losers, adopted changes, Gen4 config"
  key_links:
    - from: "results/train_step29c_mechanisms_calibrated.json"
      to: "scripts/train_step32_gen4_compound.py"
      via: "step29c winners determine step32 config stack"
    - from: "results/train_step48_kiter_sweep_d64.json"
      to: "scripts/train_step32_gen4_compound.py"
      via: "K_iter winner feeds into Gen4 base if better than K_iter=8"
    - from: "learnings/LEARNINGS_phase5_p8_arm1_arm2.md"
      to: "src/training/experiment_config.py"
      via: "Adopted winners update GA_BEST and topology_kwargs"
---

<objective>
ARM 1 (Generational Compounding) + ARM 2 (New Mechanisms): Sync running experiments (step29c, step48, step54), dispatch remaining experiments (step52, step53), compose Gen4 compound config (step32), and document the definitive Gen4 accuracy ceiling.

Purpose: Determine which mechanisms compound on the calibrated D=64 base and establish the Gen4 best configuration that future phases inherit.
Output: All ARM 1+2 result JSONs synced; Gen4 compound result; LEARNINGS documenting winners/losers and adopted config changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md
@learnings/EXPERIMENT_QUEUE.md
@learnings/LEARNINGS_phase5_p7_gen3_arm_status.md
@src/training/experiment_config.py
@src/training/trainer.py
@scripts/train_step29c_mechanisms_calibrated.py
@scripts/train_step48_kiter_sweep_d64.py
@scripts/train_step52_high_d_routing.py
@scripts/train_step54_warm_restart_lr.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync and analyze ARM 2 results (step48, step54, step52) + write step53 script</name>
  <files>
    results/train_step48_kiter_sweep_d64.json,
    results/train_step54_warm_restart_lr.json,
    results/train_step52_high_d_routing.json,
    results/train_step53_lowrank_mixing.json,
    scripts/train_step53_lowrank_mixing.py
  </files>
  <read_first>
    - .planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md (SSH details, concurrency rules, currently running PIDs)
    - learnings/EXPERIMENT_QUEUE.md (step48/52/53/54 designs with exact configs)
    - scripts/train_step48_kiter_sweep_d64.py (reference for script structure)
    - scripts/train_step52_high_d_routing.py (reference for high-D routing configs)
    - scripts/train_step54_warm_restart_lr.py (reference for LR schedule configs)
    - src/sgnnet/model_resonant.py (routing wrapper interface)
    - src/sgnnet/mechanisms_inhibitory.py (AntiHebbian wrapper interface)
    - src/training/experiment_config.py (topology_kwargs, trainer_kwargs)
  </read_first>
  <action>
**Phase A — Sync completed/running experiments from Mac Studio:**

1. Check Mac Studio experiment status:
   ```
   ssh mac-studio 'ps aux | grep "train_step" | grep -v grep'
   ```

2. Sync step54 result (nearly done per CONTEXT, e140/150):
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step54_warm_restart_lr.json results/
   ```
   If not complete yet, monitor: `ssh mac-studio "tail -5 /Users/admin/ml/dhiraj/qwen2_omni/testing/logs/train_step54_warm_restart_lr.log"`
   Wait for completion, then sync.

3. Sync step48 result (e50/150 per CONTEXT — may still be running):
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step48_kiter_sweep_d64.json results/
   ```
   If incomplete, note partial results and proceed. Come back when complete.

4. Analyze step54: Extract best LR schedule. Compare plateau vs CosineWarmRestarts vs cosine final top-1. Decision: if CosineWarmRestarts beats plateau by >= 1pp, adopt for Gen4. Otherwise keep plateau.

5. Analyze step48: For each K_iter value, record final top-1. Key questions:
   - Does K_iter=12 or K_iter=16 beat K_iter=8 (with or without AntiHebb)?
   - Does AntiHebb prevent over-smoothing at deeper K_iter (compare configs E/G vs A-D)?
   - Decision: if any K_iter > 8 + AntiHebb beats K_iter=8 + AntiHebb by >= 1pp, adopt new K_iter for Gen4.

**Phase B — Dispatch step52 and write+dispatch step53:**

6. Check Mac Studio concurrency (must be <= 1 running AND RAM >= 50GB):
   ```
   ssh mac-studio 'ps aux | grep "train_step" | grep -v grep | wc -l'
   ssh mac-studio "vm_stat | awk '/Pages free|Pages inactive/ {sum+=\$NF} END {printf \"%.0f GB\n\", sum*16384/1073741824}'"
   ```

7. Rsync step52 script (already written locally) and dispatch:
   ```
   rsync -avz scripts/train_step52_high_d_routing.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step52 'd_env/bin/python3 -u scripts/train_step52_high_d_routing.py --device mps 2>&1 | tee logs/train_step52_high_d_routing.log'"
   ```

8. Write `scripts/train_step53_lowrank_mixing.py` following the step53 design from EXPERIMENT_QUEUE.md:
   - Base: D=64 N=1024 K_iter=8 + AntiHebb alpha=0.5 (70.14% anchor)
   - 6 configs (Ref, A-F) as specified in EXPERIMENT_QUEUE:
     - Ref: AntiHebb alpha=0.5 uniform (anchor ~70.14%)
     - A: + low-rank mixing U*V^T where U,V in R^{D x 4} (rank-4)
     - B: + low-rank mixing rank-8
     - C: + frequency-pair mixing: 2x2 within each (dim_2k, dim_2k+1), D/2=32 pairs
     - D: + group mixing: 8 groups of 8 dims, 8x8 within each group
     - E: + frequency-pair mixing + AntiHebb only (C + AH anchor)
     - F: low-rank rank-4 WITHOUT AntiHebb (ablation: mixing vs inhibition)
   - Implement low-rank mixing as: `Z_mixed = Z + alpha_mix * (Z @ U @ V.T)` where U in R^{D x R}, V in R^{D x R}, applied once per K_iter step after gather-sum
   - Implement frequency-pair mixing as: for k in 0..D/2-1: Z[..., 2k:2k+2] = Z[..., 2k:2k+2] @ W_pair[k] where W_pair[k] is a learned 2x2 matrix
   - Implement group mixing as: for g in 0..7: Z[..., g*8:(g+1)*8] = Z[..., g*8:(g+1)*8] @ W_group[g] where W_group[g] is a learned 8x8 matrix
   - 150 epochs each config, batch=128, plateau LR
   - Save to results/train_step53_lowrank_mixing.json

9. When step52 finishes (or slot opens), dispatch step53:
   ```
   rsync -avz scripts/train_step53_lowrank_mixing.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step53 'd_env/bin/python3 -u scripts/train_step53_lowrank_mixing.py --device mps 2>&1 | tee logs/train_step53_lowrank_mixing.log'"
   ```

10. When step52 completes, sync and analyze:
    ```
    rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step52_high_d_routing.json results/
    ```
    Key question: does any subspace gating config (A-G) beat AntiHebb-only reference? If so, adopt into Gen4.

11. When step53 completes, sync and analyze:
    ```
    rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step53_lowrank_mixing.json results/
    ```
    Key question: does low-rank or frequency-pair mixing compound with AntiHebb? If F (no AntiHebb) is competitive, mixing may REPLACE inhibition.
  </action>
  <verify>
    <automated>python3 -c "
import os, json
missing = []
for f in ['results/train_step48_kiter_sweep_d64.json', 'results/train_step54_warm_restart_lr.json']:
    if not os.path.exists(f): missing.append(f)
if os.path.exists('scripts/train_step53_lowrank_mixing.py'):
    import ast; ast.parse(open('scripts/train_step53_lowrank_mixing.py').read())
else:
    missing.append('scripts/train_step53_lowrank_mixing.py')
if missing: print(f'MISSING: {missing}')
else: print('OK — step48/54 synced, step53 script valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - results/train_step54_warm_restart_lr.json exists locally with LR schedule comparison data
    - results/train_step48_kiter_sweep_d64.json exists locally (may be partial if still running)
    - scripts/train_step53_lowrank_mixing.py exists and parses without syntax error
    - Script references 6 configs (Ref, A-F) matching EXPERIMENT_QUEUE design
    - step52 dispatched to Mac Studio (tmux session step52 running)
  </acceptance_criteria>
  <done>ARM 2 experiments (step48, step54) synced; step52 dispatched; step53 written and queued for dispatch; analysis of K_iter and LR schedule winners documented</done>
</task>

<task type="auto">
  <name>Task 2: Sync ARM 1 results (step29c), compose Gen4 compound (step32), dispatch, and write LEARNINGS</name>
  <files>
    results/train_step29c_mechanisms_calibrated.json,
    scripts/train_step32_gen4_compound.py,
    results/train_step32_gen4_compound.json,
    learnings/LEARNINGS_phase5_p8_arm1_arm2.md,
    src/training/experiment_config.py
  </files>
  <read_first>
    - .planning/phases/05-scalable-architecture-experiments/05-CONTEXT.md (step29c status: running, PID 89895, e10/~80)
    - learnings/EXPERIMENT_QUEUE.md (step29c design: Phase 1 40ep calib + Phase 2 150ep, step32 design)
    - scripts/train_step29c_mechanisms_calibrated.py (understand the config structure and JSON output format)
    - results/train_step29_antihebb_d64.json (current best: 75.24% Config C)
    - results/train_step22b_routing_calib_d64.json (calibrated base params)
    - src/training/experiment_config.py (current GA_BEST, topology_kwargs — to be updated)
  </read_first>
  <action>
**Phase A — Sync and analyze step29c (ARM 1 mechanisms on calibrated base):**

1. Check step29c status on Mac Studio:
   ```
   ssh mac-studio "tail -20 /Users/admin/ml/dhiraj/qwen2_omni/testing/logs/train_step29c_mechanisms_calibrated.log"
   ```

2. When complete, sync:
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step29c_mechanisms_calibrated.json results/
   ```

3. Analyze step29c JSON:
   - Phase 1 (40ep calib): which alpha/tau/frac is best for each mechanism (AntiHebb, phase_exc, fast_W_phase, interneurons)?
   - Phase 2 (150ep full): for configs Ref/A/B/C/D/E/F/G, which single-mechanism beats Ref? Which combinations (E=AH+exc, F=AH+exc+int, G=ALL) compound?
   - Key metric: does any config beat the 75.24% from step29 Config C (AntiHebb alpha=0.7 on uncalibrated base)?
   - Record survival results: which mechanisms are IN, which are OUT for Gen4.

**Phase B — Compose Gen4 config from ARM 1 + ARM 2 winners:**

4. Build the Gen4 config stack by merging:
   - From step29c: surviving mechanisms and their best alphas/params
   - From step48 (Task 1): if K_iter > 8 won, use new K_iter value; else K_iter=8
   - From step54 (Task 1): if CosineWarmRestarts won, use it; else plateau
   - From step52 (Task 1): if any high-D routing config won, include it
   - From step53 (Task 1): if low-rank mixing won, include it
   - Always include: AntiHebb alpha=0.7 wpos (confirmed winner)

5. Write `scripts/train_step32_gen4_compound.py`:
   - Ref: calibrated D=64 base from step22b best params + AntiHebb alpha=0.7 (replicates current 75.24% on calibrated base)
   - Config A-G: progressive mechanism stacking as determined from step29c Phase 2 winners, PLUS any ARM 2 winners adopted in step 4
   - Each config: 150 epochs, batch=128, LR schedule from step54 winner (or plateau default)
   - Save to results/train_step32_gen4_compound.json
   - Include param count per config for cost tracking

6. Dispatch step32 to Mac Studio (check concurrency first):
   ```
   rsync -avz scripts/train_step32_gen4_compound.py mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/scripts/
   ssh mac-studio "cd /Users/admin/ml/dhiraj/qwen2_omni/testing && tmux new-session -d -s step32 'd_env/bin/python3 -u scripts/train_step32_gen4_compound.py --device mps 2>&1 | tee logs/train_step32_gen4_compound.log'"
   ```

7. When step32 completes, sync:
   ```
   rsync -avz mac-studio:/Users/admin/ml/dhiraj/qwen2_omni/testing/results/train_step32_gen4_compound.json results/
   ```

**Phase C — Update config and write LEARNINGS:**

8. Update `src/training/experiment_config.py`:
   - Set GA_BEST to the Gen4 best config (from step32 results)
   - If K_iter changed: update topology_kwargs["K_iter"]
   - If LR schedule changed: update trainer_kwargs["scheduler"]
   - Comment with provenance: "Gen4 best from step32, Phase 5 Plan 04"

9. Write `learnings/LEARNINGS_phase5_p8_arm1_arm2.md`:
   - **ARM 1 section:** step29c results table (all configs), mechanism survival analysis, Gen4 compound result
   - **ARM 2 section:** step48 (K_iter winner), step54 (LR schedule winner), step52 (high-D routing results), step53 (low-rank mixing results)
   - **Gen4 config:** final adopted stack with accuracy and param count
   - **Dead ends:** mechanisms that did NOT survive or compound
   - **Decision log:** which ARM 2 results were adopted into Gen4 and why
  </action>
  <verify>
    <automated>python3 -c "
import os
files = [
    'results/train_step29c_mechanisms_calibrated.json',
    'scripts/train_step32_gen4_compound.py',
    'learnings/LEARNINGS_phase5_p8_arm1_arm2.md'
]
missing = [f for f in files if not os.path.exists(f)]
if missing: print(f'MISSING: {missing}')
else:
    import ast; ast.parse(open('scripts/train_step32_gen4_compound.py').read())
    print('OK — step29c synced, step32 written, LEARNINGS exists')
"</automated>
  </verify>
  <acceptance_criteria>
    - results/train_step29c_mechanisms_calibrated.json exists locally with mechanism survival data
    - scripts/train_step32_gen4_compound.py exists, parses, and references the winning mechanisms from step29c
    - step32 dispatched on Mac Studio (or results already synced if fast enough)
    - learnings/LEARNINGS_phase5_p8_arm1_arm2.md exists with ARM 1 and ARM 2 sections
    - LEARNINGS documents: (a) mechanism survival table, (b) K_iter winner, (c) LR schedule winner, (d) Gen4 config
    - src/training/experiment_config.py updated with Gen4 best if step32 results available
  </acceptance_criteria>
  <done>ARM 1 mechanisms analyzed; Gen4 compound config composed from all ARM 1+2 winners; step32 dispatched; LEARNINGS documents the full ARM 1+2 investigation with adopted changes</done>
</task>

</tasks>

<verification>
- step29c, step48, step54 result JSONs exist locally
- step52 dispatched; step53 script written and dispatched
- step32 (Gen4 compound) script written from ARM 1+2 winners and dispatched
- LEARNINGS_phase5_p8_arm1_arm2.md documents all ARM 1+2 findings
- experiment_config.py updated with Gen4 best config
</verification>

<success_criteria>
Gen4 compound configuration identified from systematic ARM 1+2 investigation; all experiment results synced and analyzed; definitive Gen4 ceiling accuracy recorded. This provides the mechanism stack that Plan 05-06 synthesis uses as the "best config" candidate.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalable-architecture-experiments/05-04-SUMMARY.md`
</output>
