# Meditation 005 — 2026-07-19

**Trigger:** on-demand (user: "run the meditation")
**Period covered:** step986 → cnn_step036 (CNN GA line 006→036 + CIFAR-10 scaling step984→986 + audio/TS negatives)
**Core value:** stubborn with the goal, flexible with the approach

---

## 1. Goal Re-anchor

Goal (stubborn): improve **memory footprint (bytes)** + **energy efficiency (Joules)** of deep learning. First milestone (stubborn): **ship the paper**. Vehicle (flexible): SGNNET as FC-replacement for VGG16.

**Surrogate the last ~40 experiments optimized: MACs, FLOPs, and params.** The paper title claims *memory* and *energy*. **Bytes-on-disk and Joules-per-inference have NEVER been measured.** The CNN GA line ran 30 experiments (006→036) to shave MACs; a 30-line POC this session showed depthwise-separable convs are memory-bound (8.2× fewer MACs → 2.7× *slower* wall-time). **The surrogate ≠ the goal. This is the meditation's central finding.**

## 2. Evidence Ledger

| Step | Finding | Tag | Implication |
|---|---|---|---|
| cnn_step030 | GA5 (48,96,384) T2 = 77.79%±0.26pp, 523K params, 141.3M MACs | CONFIRMED | CNN distiller efficiency optimum found |
| cnn_step034–036 | k=5 worse than k=3 (−1.51pp); side=True no-gain; GA6 Pareto-dominated | CONFIRMED | **CNN GA architecture search COMPLETE — line converged** |
| step986 | CIFAR-10 N=16384 T2 = 84.66%±0.06pp, monotonic | CONFIRMED | **Scaling law CONFIRMED — line converged** |
| POC-C (this session) | depthwise 8.2× fewer MACs → 2.7× SLOWER wall-time (mac) | CONFIRMED | **MACs mislead latency.** CNN Pareto claim (MAC-based) unvalidated on wall-time |
| step526/527 | INT8 weight-only = −0.20pp lossless @ N=2048/D=16 | CONFIRMED | 4× memory available, NEVER applied to champion, NOT in headline |
| ffn_baseline | per-channel FFN plateaus 93.0% (1%≈5% budget); RReLU-at-train REJECTED | CONFIRMED | SGNNET +2.9pp @ 32× fewer params — grouped-FFN line CONCLUDED |
| ESC-50 / TS | audio −11.5pp gap (D=8 best); TS all → mean predictor | CONFIRMED | Multimodal negatives locked for paper (honest scope) |

**Two of the three active lines (CNN GA, CIFAR-10 scaling) are CONVERGED.** No open tactical thread is feeding the milestone. This is why 3 slots are idle.

## 3. Drift Check (goal-backward)

- **CNN GA line (006→036, 30 experiments):** Optimized MACs. Converged on GA5. **Drifted** — never measured wall-time or Joules, and POC-C says MACs mislead. Valuable science, but the efficiency claim it produces is on the wrong axis. **Reframe:** validate GA5 on wall-time before it enters the paper as an efficiency result.
- **CIFAR-10 scaling (984→986):** Serves the goal (a scaling law is a paper asset). Converged. **Continue** — done, write it up.
- **Paper writing:** LaTeX still **zero** `.tex` files after being flagged in meditations 001–004. This is the 5th consecutive flag. The milestone is "ship" and the vehicle for shipping is a compiled PDF. **This is the drift that matters most** — not a science drift, an execution drift.
- **Grouped-FFN idea (user proposed this session):** already CONCLUDED (ffn_baseline, 93.0% plateau). Re-running would be drift. See §4.

## 4. Stuck Check (approach-forward)

- **MACs as the efficiency metric (defended 30+ CNN experiments):** change-of-mind evidence = a case where fewer MACs ≠ faster/cheaper. **We now HAVE that evidence** (POC-C, 2.7× slower) and had been explaining it away as "MACs are the standard proxy." Stuck. Fix queued (cnn_step037).
- **"Champion is 5.26× faster" (single-seed, single-metric):** defended since step605. It's a wall-time claim with no Joules and no memory-bytes number behind the "memory footprint" title. Stuck on proxies. Fix queued (step997 Joules, step998 bytes).
- **Grouped/per-channel FFN (user's fresh idea):** the instinct is "new architecture, let's run it." Evidence says the line concluded at 93.0% and SGNNET already wins by +2.9pp at 32× fewer params. Defending the *urge to run it* would be stuck-on-approach. Honest call: don't re-sweep; harvest at most one baseline row.
- **Orthogonal approach avoided because "not-SGNNET":** measuring **actual Joules and actual bytes**. We've avoided it because it's benchmarking, not modeling — but it's exactly what the paper title promises. Un-avoid it.

## 5. Reframes

1. What if the paper's real contribution is **measured Joules/bytes**, and MACs/FLOPs were always a stand-in we mistook for the result?
2. What if GA5 (MAC-optimum) is **not** the wall-time optimum, and the dense control is actually faster on real hardware?
3. What if the champion's "memory footprint" claim is unbacked — no one has counted the bytes or applied the free 4× INT8 win?
4. What if the strongest iso-param baseline (a *pruned* VGG FC at 34,976 params) has never been run, leaving the headline comparison open to a reviewer's first question?
5. What if the grouped-FFN idea, which *feels* novel, is a re-run of a concluded line — and the only genuinely-new sliver (A4 aspect ratio + RReLU→ReLU anneal *schedule*) won't cross the 93.0% plateau anyway?
6. What if "ship the paper" is blocked not on science but on a single mechanical task (LaTeX) deferred 5 meditations running?

## 5b. POC Sandbox — Evidence per Reframe

| Reframe | POC run | Outcome | Notes |
|---|---|---|---|
| 1 | grep paper for "Joules"/"mJ"/"energy" measurement code — none exists; only FLOP counts | **survived** | Title claims energy; zero Joules measured. step997 built. |
| 2 | POC-C earlier this session: depthwise 8.2× fewer MACs, 2.7× slower wall-time on mac | **survived** | MACs mislead. cnn_step037 built to test GA5 vs dense wall-time. |
| 3 | analytic: 34,976 params × (4B fp32 → 1B int8) = 140KB → 35KB; step526 wrap-free recipe on hand | **survived** | 4× memory win sitting unused. step998 built (deterministic path runs now). |
| 4 | grep: no pruned-VGG-FC baseline script anywhere; sec3 lists it "NOT YET" | **survived** | Named paper hole. step999 built (global magnitude prune → fixed-sparsity fine-tune). |
| 5 | grep ffn_baseline: T1 b1_C=93.04%, b5_C=92.97% (1%≈5%, saturated); variant B (RReLU) worse both tiers | **killed** | Line concluded. A4/anneal sliver low-information vs +2.9pp SGNNET lead. Do NOT re-sweep. |
| 6 | `ls **/*.tex` → 0 files; flagged in meds 001,002,003,004 | **survived (as execution P0)** | Not a science reframe; a standing blocker. Not a queue experiment. |

Four reframes survived into measurement P0s. One (grouped-FFN) was killed by the codebase itself — logged so future meditations don't re-raise it.

## 6. Candidate Directions

| ID | Description | Cost | Expected Signal | Risk of Zero | POC Evidence |
|---|---|---|---|---|---|
| C1 | Energy Joules bench (champion vs VGG_FC), nvidia-smi P·dt | 1 slot, ~10min | HIGH — converts title to measured | low | grep: no Joules code exists |
| C2 | Wall-time vs MACs for GA5 vs dense | 1 slot, ~5min | HIGH — validates/breaks CNN Pareto | low | POC-C: 2.7× slower measured |
| C3 | INT8 champion memory + quant-MSE | 1 slot, ~2min | MED-HIGH — 4× memory claim | low | step526 lossless on hand |
| C4 | Pruned VGG FC @ 34,976 params | 1 slot, ~15min | HIGH — fills named baseline hole | med (may need tuning) | sec3 lists "NOT YET" |
| C5 | Grouped/per-channel FFN re-sweep (A4 + anneal) | 1 slot, ~20min | LOW — plateau known | HIGH | ffn_baseline concluded 93.0% |
| C6 | LaTeX conversion (sec1 abstract first) | manual, ~1hr | HIGH — unblocks ship | none | 0 .tex files |
| C7 | Champion 3-seed retrain-with-save (unblocks step998/997 accuracy) | 1 slot, ~30min | MED — kills single-seed vuln | low | step605 single-seed |

## 7. Priorities for Next 25 (ACTIONABLE)

| Step # | Title | Script file | Slot | Tier | Smoke-test | Motivation |
|---|---|---|---|---|---|---|
| step997 | Energy Joules bench | `scripts/bench_step997_energy_joules.py` | 5060ti_cuda | bench | ✅ | Measure the title's "energy" claim |
| cnn_step037 | Wall-time vs MACs | `scripts/cnn_distiller/bench_cnn_step037_walltime.py` | mini_mps | bench | ✅ | Validate CNN Pareto on real latency |
| step998 | INT8 champion memory | `scripts/train_step998_int8_champion.py` | mini_cpu | bench | ✅ | 4× memory footprint win |
| step999 | Pruned VGG FC baseline | `scripts/train_step999_pruned_vgg_fc_baseline.py` | mini_cpu | T-base | ✅ | Fill iso-param baseline hole |

**Non-experiment P0 (persistent, 5th flag):** LaTeX conversion — sec1_abstract → `.tex` first. This is the ship blocker; no experiment substitutes for it.

Close checklist:
- [x] ≥ 3 scripts written to `scripts/` (4 written)
- [x] All listed step scripts pass `--help` smoke test (4/4 OK)
- [x] `EXPERIMENT_QUEUE.md` has ≥ 3 new `QUEUED` rows (4 rows, "meditation 005" section)
- [x] `TRACKER.md` history row appended
- [x] Meditation file saved

## Parking Lot

- **C5 grouped-FFN A4+anneal:** killed by evidence (93.0% plateau). If ever revisited, run ONE T0 of the anneal-curriculum only (RReLU early → hard ReLU final epochs, distinct from the rejected RReLU-throughout) — but the plateau is architectural (budget saturates at 1%), so ceiling won't move. Deferred indefinitely.
- **C7 champion 3-seed retrain-with-save:** needed to unblock the *accuracy* half of step997/step998. Promote next meditation if the byte/Joules-only results land and the single-seed vuln is still open.
- **Audio D=8 / TS:** locked negatives — no further work, paper-scope only.

---

*Next meditation due at step (last_step + 25). TRACKER updated. Central finding: measure the goal (Joules, bytes), not the proxy (MACs, FLOPs).*
