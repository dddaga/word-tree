# --- Qdrant & Database Configuration ---
qdrant:
  url: "http://localhost:6333"
  collection_name: "run3"


# --- Graph Structure & Node Initialization ---
graph:
  total_nodes: 15454 #123633664/ (1000 * vector_dim)
  input_nodes: 3136 # 512*7*7 / vector_dim
  output_nodes: 256
  cardinality: 200
  radiation_targets: 0

# --- Model Architecture ---
model:
  vector_dim: 8
  # phase_bins: null
  # mag_bins: null
  # gamma: 1.5
  iterations: 5
  radiation_similarity_threshold: 0.1
  temporal_decay: 1.0
  activation_threshold: 0.1
  dtype: "float32"
  scattering_prob: 0.0
  stochastic_radiation_duration: 0.4
  layernorm: true
  # DOCUMENTARY — not read by current code. This run used the legacy in-function
  # mean-subtraction in update_activations. To reproduce exactly, check out
  # git commit 7f072c4 on branch fixed_io_nodes. See:
  # vgg_training/learnings/concepts/mag_normalization.md
  subtract_mean: true
  dropout: 0.0

# --- Training Hyperparameters ---
training:
  lr: 0.0001
  batch_size: 4
  accumulation_steps: 4
  epochs: 20
  validation_fraction: 0.0


# --- System & Logging ---
system:
  device: "mps"
  random_seed: 42
  weights_save_path: "training_runs/run3/run3_weights.pt"
  tensorboard_dir: "training_runs/run3/tensorboard"
  logging:
    log_path: "training_runs/run3/run3.csv"
    save_interval_seconds: 10
    fieldnames: ['worker_id', 'loss', 'class', 'skipped']
    verbose: false
